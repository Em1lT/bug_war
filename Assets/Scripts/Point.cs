using UnityEngine;
using TMPro;

public class Point : MonoBehaviour
{

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // score_text.text = score.ToString();
    }

}
